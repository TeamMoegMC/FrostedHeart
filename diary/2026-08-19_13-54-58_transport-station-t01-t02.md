# Transport Station T01 and T02

- Time: `2026-08-19 13:54:58 +08:00`
- Author: `Codex; OpenAI GPT-5; primary implementation agent`
- Status: `completed`
- Scope: `town transport-station building persistence and space configuration`

## Completed

- Added `TransportStationBuilding` with persistent geometry, resident roster, value-guarded setters, inactive foundation-stage work, and a fixed zero resident score.
- Registered the stable `transportStation` polymorphic building key, with a deferred codec wrapper to prevent the existing abstract-codec static initialization cycle and without shifting legacy integer type indexes.
- Added transport-station area, volume, and worker-slot defaults plus `FHConfig.SERVER.TOWN.TRANSPORT_STATION`.
- Guarded `AbstractTownResidentWorkBuilding#setMaxResidents` against redundant building-change events.
- Added codec, change-event, and configuration-default tests.

## Decisions

- This milestone persists only building state. Block registration, structural scanning, UI, and all transport-capacity production remain deferred to their planned tasks.
- Resident UUIDs are encoded in UUID sort order so equivalent rosters have deterministic serialized output.

## Validation

- `gradle test --tests 'com.teammoeg.frostedheart.content.town.buildings.logistics.*' --offline --no-daemon --console=plain` completed successfully: 6 tests, 0 failures.
- `gradle test --tests 'com.teammoeg.frostedheart.content.town.model.TownModelParameterDefaultsTest' --offline --no-daemon --console=plain` completed successfully.

## Remaining

- Continue with T03: block, block entity, and structural scanning.
