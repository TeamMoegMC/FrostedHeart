# Scope ModMaven to historical Create artifacts

- Time: `2026-08-09 23:23:32 UTC+8`
- Author: `Codex; OpenAI GPT-5; primary coding agent`
- Status: `completed`
- Scope: `build.gradle`, snapshot data-generation dependencies

## Completed

- Restored the original Maven development dependencies for Create 0.5.1.j, Flywheel
  0.6.11-13, and Registrate 1.3.3 so IDE source attachment remains available.
- Kept ModMaven but restricted it to `com.simibubi.create`, `com.jozufozu.flywheel`, and
  `com.tterrag.registrate`.
- Removed the temporary Curse Maven Create dependency and bundled-Flywheel extraction task.

## Decisions

- ModMaven is not fully unavailable: the required Create-family binaries and source JARs
  are valid, while some unrelated missing artifacts are returned as small HTML documents
  with HTTP 200. Repository content filtering prevents those false responses from masking
  the official Forge and Mojang repositories.
- Preserve the previous diary entry as a record of the intermediate Curse Maven workaround;
  this entry supersedes that approach.

## Validation

- Directly verified valid binary and source archives for all three allowed groups.
- Ran `GRADLE_USER_HOME=/tmp/frostedheart-codex-gradle-scoped-modmaven-20260809
  ./gradlew runData -Pmod_version="0.7.6-snapshot882" --stacktrace --no-daemon` from a
  completely new Gradle cache; it completed with `BUILD SUCCESSFUL` in 8m 57s.
- `git diff --check` passed, and data generation produced no tracked resource changes.

## Remaining

- Confirm the pushed `Publish Snapshots` workflow completes its packaging, upload, and
  publication steps. The dependency-resolution and `runData` failure is locally resolved.
