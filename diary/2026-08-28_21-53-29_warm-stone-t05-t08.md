# Warm stone state, items, and Curios foundation

- Time: `2026-08-28 21:53:29 +08:00`
- Author: `Codex; OpenAI GPT-5; primary engineering agent`
- Status: `completed`
- Scope: `wearable reservoir ItemStack persistence, item registration, Curios slot contract, and climate documentation`

## Completed

- Added version-one `WearableThermalState` persistence under `frostedheart:thermal_reservoir`, with separate core/surface absolute temperatures and a server-environment-only repair/initialization path.
- Added the no-capability `WearableThermalReservoir` contract and generic single-stack `WarmStoneItem`, then registered `warm_stone` and `hot_water_bag` with the frozen default profiles.
- Registered the one-slot `warm_stone` Curios type and its exact slot-zero gameplay query. Visibility and render presentation flags do not alter that query.
- Added state, item-contract, and Curios handler tests; documented the current persistence and runtime boundary in the climate living docs.

## Decisions

- Invalid reservoir state is repaired only with a finite server environment sample, clamped to `[-1000,1000] degC`; otherwise it remains unavailable and unchanged for the current tick.
- The slot priority is `190`, between the repository's existing belt and charm presets. The slot icon is registered by resource location now, while its asset remains T09 work.
- Curios API is present on production `compileOnly` but not inherited by the test source set, so the matching API was added as `testCompileOnly` only.

## Validation

- JDK 17 targeted T05-T08 tests passed `11/11` across `WearableThermalStateTest`, `WarmStoneItemTest`, and `CuriosCompatWarmStoneTest`.
- JDK 17 direct T00-T08 selection passed `38/38` across `10` suites.
- `compileJava` completed after rebuilding stale main output. The first cold test run encountered generated Gradle class-output and result-file locking issues; a daemon stop followed by `--no-daemon` runs yielded the passing results.

## Remaining

- T09 must add tags, translations, models, textures, the empty-slot icon asset, and tooltip without client NBT writes.
- T10-T12 must connect player/inventory runtime exchange, count NBT writebacks and packet behavior for Gate B. Dropped-item/world runtime remains T13-T19 work.
