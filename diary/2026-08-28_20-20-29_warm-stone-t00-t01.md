# Warm stone T00-T01 baseline and profile contract

- Time: `2026-08-28 20:20:29 +08:00`
- Author: `Codex; OpenAI GPT-5; primary engineering agent`
- Status: `completed`
- Scope: `warm-stone implementation baseline and immutable wearable thermal profile`

## Completed

- Verified the player temperature update order, core-temperature ownership, reusable thermal exchange primitives, Curios 5.9.1 slot/sync semantics, Forge ItemEntity hook, thermal test baseline, and both repository states.
- Added `WearableThermalProfile` with exactly four authoritative parameters and source-default profiles for `warm_stone` and `hot_water_bag`.
- Derived inventory and dropped environmental conductance from `playerTransferRatePerSecond`; no Forge capability, NBT state, item registration, gameplay hook, or second solver was introduced.
- Marked plan tasks `T00-T01` complete and appended a structured checkpoint to the shared handoff.

## Decisions

- Curios `isVisible()` and `getRenders()` are both presentation state and will not control warm-stone exchange; T08 will use exact `warm_stone` handler and slot `0` existence.
- `SlotTypeMessage` remains the current repository compatibility baseline despite its Curios 1.20.1 deprecation; T08 owns the actual slot registration change.
- Runtime ItemStack values remain repairable state for T05. Constructor rejection in `WearableThermalProfile` applies only to source-defined immutable developer profiles.

## Validation

- Java 17 Gradle profile test passed `4/4`.
- Profile, `ThermalExchangeKernelTest`, and `TemperatureComputationRadiationTest` passed `10/10` together.
- Existing thermal runtime JUnit passed `187/187` across `33` suites.
- An initial ForgeGradle mapped JAR regeneration was interrupted by a Windows file lock; the generated 22-byte workspace cache artifact was removed after the lock released and regenerated in place. No dependency copy or alternate path-sensitive build was created.

## Remaining

- Gate A remains open. Implement `T02` and `T03` against the frozen profile, then complete `T04` curve and radiation-delegation validation.
- Living climate documentation needs no update until the profile is connected to implemented player or item behavior.
