# P2P Transport T01 Pure Model

- Time: `2026-08-23 23:52:59 +08:00`
- Author: `Codex; OpenAI GPT-5; implementation and validation role`
- Status: `completed`
- Scope: `P2P direct-link transport kind, formula, parameters, paused reservation state, tests, and living documentation`

## Completed

- Added the sender-owned `TransportEndpointKind.P2P_DIRECT_LINK` and stable Codec coverage.
- Added same-dimension `GlobalPos` Manhattan distance, the independent P2P distance factor, and P2P capacity dispatch to
  `TransportReservationModel` without Forge or block-entity dependencies.
- Added `p2pDistanceCostPerBlock` to source defaults, Forge server configuration, model snapshots, and stage-zero audit output.
- Added `TransportAdmissionStatus.REDSTONE_PAUSED` with persisted non-zero rate/metric and mandatory zero-capacity invariants.
- Guarded the existing warehouse-only `TeamTown` registration and metric-refresh paths against the new P2P kind.
- Updated the P2P source/task plans and the two living town documents affected by the model and configuration change.

## Decisions

- One `P2P_DIRECT_LINK` reservation kind covers both a shipping terminal and a bidirectional terminal acting as sender; device role
  and receiver indexes remain binding-state concerns for T03.
- P2P and warehouse distance coefficients have separate parameter/config fields even though both source defaults are `0.05`.
- `REDSTONE_PAUSED` is P2P-only and distinct from zero-rate `DISABLED` and fact-unavailable `UNAVAILABLE`.
- T01 does not expose a P2P mutation entry point. The current warehouse facade rejects P2P before deriving warehouse topology facts.

## Validation

- Failure-first targeted compile: failed with the expected 29 missing T01 symbols/constructor changes before implementation.
- Focused model/state/parameter matrix: `42` tests, zero failures/errors/skips.
- `*Transport*`: `89` tests, zero failures/errors/skips.
- `com.teammoeg.frostedheart.content.town.*`: `370` tests, zero failures/errors/skips.
- Full `test compileJava`: `495` tests, zero failures/errors/skips; compile succeeded under Java 17.
- `git diff --check`: passed. `runData` was not needed because no generated resource changed.

## Remaining

- T02 must split warehouse and P2P authoritative fact resolution from the shared admission core.
- T03 must implement binding persistence, batch reservation transactions, and load-time distance revalidation.
- P2P blocks, transfer, buffers, menus, synchronization, and game validation remain T04-T10/H01 work.
