# Live thermal JFR spot check

- Time: `2026-08-27 20:32:24 +08:00`
- Author: `Codex; OpenAI GPT-5; primary profiling agent`
- Status: `completed`
- Scope: `run/thermal-live-20260827.jfr`, current Forge client JVM thermal runtime sampling

## Completed

- Attached JFR to the running Java 17 game client (PID `13480`) with `settings=profile`, `duration=180s`, and wrote `run/thermal-live-20260827.jfr`.
- Recording completed and was parsed offline after the client process exited. The file is approximately 6.9 MB and covers 161 seconds.
- Separated Server thread samples from Render thread samples so spatial weather rendering was not counted as server thermal work.

## Decisions

- Treat this as a spot-check baseline, not a benchmark. The player workload was not scripted and Page count, arena sparsity, source count, and mutation count were not controlled.
- Do not implement a traversal optimization from this recording alone. Use the observed thermal allocation/topology signals to design the next controlled capture.
- Keep the existing runtime contract: thermal work is on the server thread; no active `TemperatureThreadingPool` or thermal executor was observed or enabled.

## Validation

- JFR summary: 2,245 `jdk.ExecutionSample`, 27,572 `jdk.ObjectAllocationSample`, 158 `minecraft.ServerTickTime`, and 20 G1 garbage-collection events.
- Server thread: 679 execution samples total; 64 samples contained thermal frames (9.4% of Server thread samples, 2.9% of all execution samples). The most frequent thermal leaf samples were `ConservativeAirGeometry.Resolution.componentAt` (20), `airMicrocellIfPresent` (20), and `compileMaterialFragment` (5).
- Server tick duration: average `4.848 ms`, P95 `6.56 ms`, maximum `7.85 ms` across 158 events.
- Server thermal allocation samples: 489 events. The largest estimated sampled classes were `int[]` 64.12 MB, `double[]` 61.36 MB, `ThermalSweep.PairOperation` 26.90 MB, and `MaterialPoleKey` 25.43 MB. JFR allocation weights are estimates, not exact allocation totals.
- GC pause sum was `208.49 ms` across 20 events, P95 `16 ms`, maximum `17.9 ms`. G1 old-event duration is not equivalent to stop-the-world pause time.
- No new JVM crash event was present in the recording. Attach status became unavailable after the recording ended because the game process had exited.

## Remaining

- Capture a controlled large-save baseline with fixed Page/arena/source counts and scripted mutations, movement, unload/reload, and source rebind.
- Add or expose temporary counters for `Pactive/Pslot/C/H`, publication copied slots, capture/full rebuild count, accumulator visits, and state-set sum visits so JFR samples can be correlated with actual work.
- Compare the next recording against this file before selecting live-slot publication, accumulator indexing, structural-revision preflight, or allocation reuse as the first implementation.
