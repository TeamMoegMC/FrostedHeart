# Warm stone T02-T04 pure models and Gate A

- Time: `2026-08-28 21:01:03 +08:00`
- Author: `Codex; OpenAI GPT-5; primary engineering agent, with T02/T03 sub-agents`
- Status: `completed`
- Scope: `wearable reservoir pure exchange models, shared radiant equivalent temperature, and Gate A curve fixtures`

## Completed

- Added `ThreeNodeWearableHeatExchange` for conservative core/surface/player exchange with caller-owned result and scratch carriers.
- Added `ReservoirEnvironmentExchange` for inventory and dropped core/surface/environment exchange, plus the shared `RadiantEquivalentTemperature` conversion.
- Delegated `TemperatureComputation.radiantFeelingTemperatureDelta` to the shared radiant helper while leaving the separate body-energy formula unchanged.
- Added a deterministic CSV snapshot fixture for both profiles across worn, inventory `q=0`, and dropped `q=100 W/m2` scenarios, then closed Gate A in the plan and shared handoff.

## Decisions

- Both topology orchestrators use symmetric splitting with a maximum `1 s` substep and reuse `ThermalExchangeKernel`; neither owns an exponential solver or stable-path collection state.
- The curve snapshot uses fixed `0/60/300/900/1800 s` checkpoints and six-decimal output. Its values verify the frozen profile and radiation parameters and do not retune them.
- Living climate docs were not changed because T02-T04 add pure models and tests only; no ItemStack, player, Curios, or world lifecycle behavior is implemented yet.

## Validation

- Java 17 T00-T04 direct test selection passed `27/27` across `7` suites.
- Java 17 expanded climate thermal selection passed `209/209` across `39` suites, including the existing `100 W/m2 -> 13.333333 degC` player regression.
- Static scan found no copied exponential kernel or production collection state in either topology orchestrator.

## Remaining

- Gate A is closed. Phase 1 still needs thermal ItemStack state, the two items, the Curios slot, player data/update integration, and its Gate B validation.
- Real Campfire/item receiver behavior remains intentionally deferred to T13-T19 and Gate C.
