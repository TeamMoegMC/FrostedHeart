# Probe-Compiled Sparse Conservative Thermal Graph Architecture Plan

- Time: `2026-08-22 16:58:26 +08:00`
- Author: `Codex; OpenAI; coding agent`
- Status: `completed`
- Scope: `plans/2026-08-22_16-47-30_probe-compiled-sparse-conservative-thermal-graph.md`

## Completed

- Added a pre-implementation technical architecture for an event-driven, probe-compiled sparse conservative thermal graph.
- Defined the fixed natural, analytic-field, and local-transport backend boundaries; typed source semantics and units; sparse graph nodes and edges; deterministic DDA validation; revision witnesses; thread ownership; immutable snapshots; conservative energy integration; gameplay-level convection; and an optional mass-conserving airflow extension.
- Specified campfire occlusion and power channels, geothermal shaft behavior, Generator and moving Boss integration, overload degradation, performance accounting, failure modes, migration phases, benchmarks, and production gates.

## Decisions

- Stored the proposal under `plans/` rather than `docs/` because none of the architecture is implemented current-system truth.
- Prohibited full section scans and section copies in normal operation; a section or smaller spatial key may only act as revision metadata or an index address.
- Limited probes to candidate discovery. Every published transport or visibility relation requires deterministic geometry validation and revision witnesses.
- Chose conservative buoyant mixing as the first production convection model. Pressure-corrected mass flow remains an optional enhancement inside the same sparse graph and is not a production-migration prerequisite.
- Preserved the existing player body model, legacy heat-area behavior, and heat-network gameplay units behind explicit compatibility adapters.

## Validation

- Verified all five relative Markdown links in the architecture document resolve.
- Verified all 76 fenced-code markers are balanced and an explicit content scan found no trailing whitespace in the untracked plan.
- Reviewed cross-references, dimensional units, source-mode composition, block-change classification, failure degradation, and prototype/production status wording.

## Remaining

- Implement Phase 0 only after approval: freeze query/source contracts, build a pure-Java reference solver, and establish reproducible current-system performance baselines.
- Resolve the benchmark-dependent questions recorded in the plan before production integration.
- No living `docs/climate/` update is required yet because runtime behavior did not change.
