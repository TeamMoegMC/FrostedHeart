# 新知识系统设计文档

```
Athwart the Gehlon roars a squall, 
Beneath the tree do roamers stall. 
When to becrawl, a windfall.
```

新研究系统的基底是知识系统。每个玩家所在的队伍持有并维护「知识元素」 KnowledgeElement构成的集合。我们先不为这些元素之间添加关系。
所有被允许存在的知识元素定义都由数据包定义，形成注册表，服务端维护权威，客户端维护快照。服务端和客户端在存档加载，数据包重载等重要节点同步。
游戏开始时，队伍可拥有一定数量的初始知识元素。
知识元素被加入集合的过程称为「学习」，学习是极其通用的方法。此方法可被游戏中其他系统自由调用，如指令。
知识元素被从集合移除的过程称为「忘记」。同样可由指令等其他系统调用。
知识元素的学习和忘记由游戏中的其他系统实现。
知识元素的学习和忘记应实现Forge事件系统，作为暴露给其他系统和模组的API。

我们此时具体化一点，开始定义几种基础的知识元素类型：

## 观察

第一种是「观察」Observation：某时间和位置记录的世界信息。

记录位置即该观察发生在游戏内的坐标。

记录时间即该观察发生的游戏内的时刻（也包含WorldClockSource系统得到的年月日小时等方便反馈给玩家的日期信息）。这决定了观察的独一性。

由于玩家和Minecraft世界交互的基本对象是方块、实体，根据「观察类型」ObservationType定义最基本的几种观察：

- 「方块观察」BlockObservation：方块的类型及状态（BlockState）。
- 「实体观察」EntityObservation：实体的类型。

以上信息类型不能同时存在于一个观察中。但从时间和空间信息可得到如下可附加的信息类型，我们称之为「观察上下文」ObservationContext，以下并不囊括所有可能：
- 气候事件：该坐标和时刻正在发生的气候事件类型。
- 温度类型：该坐标和时刻的方块温度，分段按照类型分为极寒、寒冷、舒适、炎热、极热。
- 生物群系：该坐标对应的Biome。
- 一天中的时间段：清晨、早上、中午、下午、傍晚、午夜、凌晨。

还有一类信息，我们称之为「场信息」Field：信息是基于时空坐标得到的数值信息。

- 方块温度（WorldTemperature.block）。
- 以及未来可能出现的其他基于时空坐标的数值信息。

这类信息由于采取数值而非类型，取值范围和多样性极大。

观察的知识性本身是对于世界中所发生事件的一种存在性证明。举例：野鸡只能生成在林地群系，所以玩家*一般情况下*能收集到的观察只会是所有可能的由野鸡作为实体类型的观察组成的集合中的一个子集，即在林地标签的群系中才会观察到野鸡的存在。但注意我强调了*一般情况下*，而非所有情况。由于Minecraft游戏系统的自由性，自然生成的野鸡可能会被人为转移、繁衍，所以玩家可能收集到子集之外的观察。这是被允许的，但这也引入了下面的机制。

注意，观察本身由于包含时空信息，所以独一无二。由于这种独一性，从数据维护角度考虑，我们也不能允许玩家在自己的知识元素集合中一直添加观察。更重要的是，我们无法用数据包穷尽所有可能的观察（游戏刻和方块坐标都趋近于无限）。因此，所有可能的观察组成的集合中，只有一部分是真正会有意义的（后续我们讲述知识组合规则的时候会讲到）。从可视化角度考虑，我们也不可能将巨量的但可能在意义上是重复的观察全部集合。

但同时，观察本身应该具有自由性。玩家可从如遗迹中发现的某物品、NPC、交易、甚至其他玩家处获取包含指定类型和上下文的观察，但是玩家自身本身的观察是自由的。

### 研究笔记和观察行为

因此，观察本身应首先作为物品形式存在，我们将存储任何观察的物品称为「研究笔记」。研究笔记可作为观察的载体在世界中存储和传播。
研究笔记可直接通过带参数的指令生成，可以由世界生成过程中动态在存储器内生成等。
研究笔记可以是空的，并可以用两张纸简单合成，并可堆叠。带有观察的研究笔记具有数据独一性，不可堆叠。

研究笔记可以由观察行为产生。玩家右手持研究笔记，长按想要研究的方块或实体，过程中若松开则代表放弃该观察行为，若完成则自动打开研究笔记界面，类似原版书籍界面，内包含观察时间、观察地点、观察类型和观察上下文。界面最下端有一个文本输入处，前缀为「笔记备注」。这里玩家可以自由为这项观察添加文字描述，仅做一个宽松的字数限制，后续会加入Tooltip物品描述。最后还有一个「笔记标题」。这里玩家可以为笔记起物品名。标题和备注都不是必须的，可直接跳过，系统则会根据观察类型和上下文，生成确定性的可翻译标题。备注则默认为空。观察时间、地点、类型和上下文等也会存在于物品描述中。

带有观察的研究笔记可以打开，玩家可以查看其中的内容，且可以再次修改标题和备注。

### 研究笔记的学习

创造或收集研究笔记并不会直接将其包含的观察加入玩家队伍的知识元素集合中。研究笔记必须经由绘图台检索，才能进入知识元素集合。这一过程将对研究笔记内包含的独一观察进行类型化降维。举例：一天内同一时间段但不同时间刻且其他上下文和类型都完全相同的两项观察，将不允许作为不同的知识元素存在于玩家队伍中。

### 经由非研究笔记途径获取的观察

游戏中，可能存在NPC、Boss或遗迹方块。这些对象可能通过对话的方式，直接向玩家分享他们曾经的或存储的观察。这种方式得到的观察将直接默认经由检索过程，存入队伍知识元素集合。

## 灵感/想法

一项「灵感」的出现意味着一条潜在的通向某些研究成果的路径被玩家发现了。

### 灵感的产生

#### 联络

灵感的第一种也是最初的获取方式便是在已有的知识元素（观察、灵感、或研究成果）中寻找联络。玩家在绘图台内将至少两个，至多五个集合中已经存在的知识元素组合在一起。若该组合中存在的元素满足某数据包内定义的「联络规则」，则联络满足，灵感诞生。联络规则的数据包内将定义该灵感的标题和文本信息，以及最重要的，该灵感可以触发的「研究项目」（可能没有，也可以有多个）。

### 灵感的暗示

由于「联络规则」在被发现之前对玩家不可见，玩家可借由以下的方式获取关于联络规则的暗示。

#### 梦境

在睡前，玩家可选对某项知识元素进行思索。睡后，将触发名为「联想」的过程：寻找并提示玩家数据包定义中与该知识元素有关的「联络规则」。如睡前不对任何知识元素进行思索，则将随机选取一个知识元素集合中的知识元素进行联想。

#### 讨论

与任何玩家或教育等级足够高的居民进行交互，可以触发讨论模式。讨论可以选择某项知识元素为主题，完成后将触发「联想」。每个玩家和居民只能在一天内成为一次讨论对象。

#### 研究所

当知识元素的集合开始变得庞大，仅靠玩家个人将越来越难在庞大的组合空间内发觉那些在发现之前对玩家不可见的「联络规则」。玩家可将灵感产生的任务交由研究所内的城镇居民进行。





## 研究成果

### 发现（理论）

### 设计（配方）

### 建造（成型）

### 流程（使用）

### 原型（升级）

### 研究成果的流通
